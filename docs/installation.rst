.. highlight:: shell

============
Installation
============

At the command line::

    $ easy_install pydots

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv pydots
    $ pip install pydots
